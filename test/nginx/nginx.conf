events {}

http {
    server {
        listen 80;
        server_name localhost;

        # Serve static files
        location / {
            root /usr/share/nginx/html;  # Directory for static files
            index index.html;
        }
    }
}
